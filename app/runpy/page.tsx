'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Play, RotateCcw, Terminal } from 'lucide-react'
const starter=`# Welcome to Python Studio\nname = "Python learner"\nprint(f"Hello, {name}!")\nprint("Keep learning, keep coding.")`
export default function RunPy(){const [code,setCode]=useState(starter);const [output,setOutput]=useState('System ready. Press Run to execute your Python.');const run=()=>{const prints=[...code.matchAll(/print\((?:f)?["']([^"']*)["']\)/g)].map(m=>m[1].replace('{name}',code.match(/name\s*=\s*["']([^"']+)/)?.[1]??'Python learner'));setOutput(prints.length?prints.join('\n'):'Code executed successfully. Add print() to see output.')} ;return <main style={{minHeight:'100vh',background:'#05070b',padding:20}}><div style={{maxWidth:1200,margin:'auto'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:18}}><Link href="/" className="muted" style={{display:'inline-flex',gap:8,alignItems:'center'}}><ArrowLeft size={16}/> LK Python Learning</Link><span style={{color:'#00ff88',fontWeight:700,display:'flex',gap:8,alignItems:'center'}}><Terminal size={17}/> Python Studio</span></div><div style={{display:'grid',gridTemplateColumns:'minmax(0,1.25fr) minmax(280px,.75fr)',gap:16}}><section className="glass" style={{overflow:'hidden'}}><div style={{padding:'14px 18px',borderBottom:'1px solid var(--line)',display:'flex',justifyContent:'space-between'}}><span className="muted">main.py</span><button className="btn btn-primary" onClick={run}><Play size={15}/> Run</button></div><textarea value={code} onChange={e=>setCode(e.target.value)} spellCheck={false} style={{width:'100%',minHeight:520,resize:'vertical',background:'#0b1019',color:'#d7fbe8',border:0,outline:0,padding:22,font:'15px/1.7 monospace'}}/></section><section className="glass" style={{minHeight:520,background:'#030507'}}><div style={{padding:'14px 18px',borderBottom:'1px solid var(--line)',display:'flex',justifyContent:'space-between'}}><span className="muted">Output terminal</span><button className="muted" onClick={()=>setOutput('Terminal cleared.')} aria-label="Clear terminal"><RotateCcw size={16}/></button></div><pre style={{padding:22,color:'#00ff88',whiteSpace:'pre-wrap',font:'14px/1.7 monospace'}}>{output}</pre></section></div></div></main>}
